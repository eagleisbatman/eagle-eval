langdetect>=1.0.9
pyyaml>=6.0
click>=8.0
tabulate>=0.9

# Optional integration SDKs are managed through pyproject extras:
#   python -m pip install -e ".[langfuse,gemini]"
#   python -m pip install -e ".[openai]"
#   python -m pip install -e ".[anthropic]"
#   python -m pip install -e ".[langsmith]"
#   python -m pip install -e ".[vertex]"
#   python -m pip install -e ".[bedrock]"
